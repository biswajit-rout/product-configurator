const initialState = {
    productData: null
}

const productReducer = (state = initialState, {type, payload}) => {
    switch(type) {
        default: 
            return state;
    }
}

export default productReducer;